# Copie para .env caso queira testar o build de produção localmente.
# Em desenvolvimento normal (npm run dev) NÃO precisa deste arquivo —
# o Vite usa o proxy configurado em vite.config.js.

# URL do backend hospedado no Render (sem barra no final).
# Ex: https://markfruit-api.onrender.com
VITE_API_URL=
