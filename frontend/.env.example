# Copie para .env e preencha com os dados do seu projeto Supabase
# (Settings > API no painel do Supabase).

# URL do projeto (ex: https://xxxxx.supabase.co)
VITE_SUPABASE_URL=

# Chave pública "anon" / "publishable" (segura de expor — o RLS protege os dados)
VITE_SUPABASE_ANON_KEY=
